rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Cualquier persona puede LEER comentarios
      allow read: if true;
      // Solo usuarios autenticados (incluido anónimos) pueden ESCRIBIR
      allow write: if request.auth != null;
    }
  }
}
